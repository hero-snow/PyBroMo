[project]
name = "pybromo"
version = "0.8.1+forked.2025.10"
description = "A simulator for single molecule FRET experiments of freely diffusing particles."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bleach>=6.3.0",
    "jupyterlab>=4.4.10",
    "matplotlib>=3.10.7",
    "nbconvert>=7.16.4",
    "phconvert>=0.9.1",
    "ruff>=0.14.8",
    "tinycss2>=1.5.1",
]

[dependency-groups]
dev = [
    "cython>=3.1.6",
    "fretbursts",
    "pybromo",
    "ruff>=0.14.3",
]

[tool.uv.sources]
fretbursts = { git = "https://github.com/OpenSMFS/FRETBursts.git" }
pybromo = { workspace = true }

[tool.uv.workspace]
members = [
    ".",
    ".",
]
